// /claim — Founding Card apply form only (name / school / email + delivery).
// Colorway is chosen on the main landing; learn-more content lives at /card.
import FschoolAILanding from "../FschoolAILanding";

export default function Claim() {
  return <FschoolAILanding mode="claim" />;
}
